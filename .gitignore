# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/

# Secrets
.env
*.token
hf_token.txt

# Heavy artifacts (live on Google Drive, not in git).
# Leading slash = repo-root only, so it does NOT match the src/models/ package.
/models/
*.safetensors
*.ckpt
*.pt

# Dataset raw/processed images
datasets/*/raw/
datasets/*/processed/
datasets/*/captions/

# Outputs
outputs/lora/
outputs/generated/
outputs/logs/

# Caches
/cache/
.cache/

# Web app runtime data (jobs + downloaded images) and staged LoRA upload
webapp/data/
kaggle/lora-dataset/lora.safetensors
