# Core
torch>=2.3.0
torchvision>=0.18.0

# Diffusion / training stack
# >=0.32 for the native BitsAndBytesConfig + per-component quantization used to
# load FLUX's transformer in 4-bit at generation time.
diffusers>=0.32.0
# Florence-2's remote code (config + modeling) is incompatible with the
# transformers>=4.50 attention/generation refactor (forced_bos_token_id removal,
# missing _supports_sdpa). Pin below it; this range is fine for FLUX too.
transformers>=4.44.0,<4.50.0
peft>=0.12.0
accelerate>=0.33.0
# 4-bit NF4 quantization of the FLUX transformer (low-VRAM generation).
bitsandbytes>=0.43.0
safetensors>=0.4.3
sentencepiece>=0.2.0
protobuf>=4.25.0

# Image / data
Pillow>=10.3.0
numpy>=1.26.0

# Captioning (Florence-2)
einops>=0.8.0
timm>=1.0.7

# Config / utils
PyYAML>=6.0.1
tqdm>=4.66.0
huggingface_hub>=0.24.0
